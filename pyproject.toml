[tool.poetry]
name = "buildingsync-preimporter"
version = "0.1.0"
description = "BuildingSync Preimporter"
authors = ["Katherine Fleming <katherine.fleming@nrel.gov>"]
license = "BSD4"

readme = "README.md"

homepage = "https://buildingsync.net"
repository = "https://github.com/BuildingSync/BuildingSync-preimporter"
documentation = "https://github.com/BuildingSync/BuildingSync-preimporter"
keywords = ["BuildingSync", "Building Data Exchange"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
